╔══════════════════════════════════════════════════════════════╗
║           CORS ПРОБЛЕМА РЕШЕНА ЧЕРЕЗ VITE PROXY              ║
╚══════════════════════════════════════════════════════════════╝

Я настроил Vite proxy чтобы обойти CORS ошибку.

Теперь запросы будут идти так:
  Frontend → http://localhost:8080/api/auth/login
  Vite Proxy → https://tourderussie.ru/api/auth/login
  
Браузер думает что запрос идёт на тот же домен = нет CORS!

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  ОБЯЗАТЕЛЬНО ПЕРЕЗАПУСТИТЕ DEV SERVER!                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

1. Ctrl+C в терминале
2. npm run dev
3. Попробуйте зарегистрироваться снова

Теперь должно работать!

═══════════════════════════════════════════════════════════════

ЧТО ИЗМЕНИЛОСЬ:

✓ vite.config.ts - добавлен proxy для /api
✓ .env.local - VITE_API_URL теперь пустой (используем proxy)
✓ api.ts - baseURL теперь '/api' (относительный путь)

Все запросы теперь идут через Vite proxy → нет CORS ошибок!

